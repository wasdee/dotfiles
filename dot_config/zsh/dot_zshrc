autoload -U compinit
compinit


eval "$(/home/ben/.local/bin/mise activate zsh)"
eval "$(mise completion zsh)"

eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
eval "$(oh-my-posh init zsh --config ~/.config/zsh/theme.omp.json)"

if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi

# export ZELLIJ_AUTO_ATTACH=true
eval "$(zellij setup --generate-auto-start zsh)"


source <(fzf --zsh)
export PATH="/snap/bin:$PATH"
export PATH="/home/ben/.bun/bin:$PATH"
export VISUAL="code --wait"
export EDITOR="micro"

# export DOROTHY="/home/ben/.local/share/dorothy"
# export PATH="/home/ben/.local/share/dorothy/commands:/home/ben/.config/dorothy/commands:/home/ben/.local/share/dorothy/commands.beta:$PATH"


# Generated for envman. Do not edit.
# [ -s "$HOME/.config/envman/load.sh" ] && source "$HOME/.config/envman/load.sh"

# Generated by Webi. Do not edit.
# eval "$(webi --init zsh)"



export PATH="$PATH:/home/ben/.modular/bin"


alias e="edit"

# $(pitchfork activate bash)

# eval "$(zoxide init bash)"
if [ -x "$(command -v supabase)" ]; then
    source <(supabase completion zsh)
fi

if [ -x "$(command -v zoxide)" ]; then
    eval "$(zoxide init --cmd cd zsh)"
fi

# rclone
if [ -x "$(command -v rclone)" ]; then
    eval "$(rclone completion zsh)"
fi

eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"

bindkey '^H' backward-kill-word


# https://github.com/marlonrichert/zsh-autocomplete
# https://github.com/roberte777/zesh
export FZF_DEFAULT_COMMAND="fd --type file --color=always"
export FZF_DEFAULT_OPTS="--ansi"
-
# -# mise npm-bun
# -export PATH="/home/ben/.local/share/mise/installs/npm-svelte-language-server/0.17.12/bin:$PATH"
# -export PATH="/home/ben/.local/share/mise/installs/npm-tailwindcss-language-server/0.14.16/bin:$PATH"
# -export PATH="/home/ben/.local/share/mise/installs/npm-olrtg-emmet-language-server/2.6.1/bin:$PATH"
# -export PATH="/home/ben/.local/share/mise/installs/npm-vscode-languageservers-extracted/4.10.0/bin:$PATH"
# -
# -alias claude="/home/ben/.claude/local/claude"
# -export POSTGRESTOOLS_BINARY="/home/ben/Devs/Tools/postgrestools"